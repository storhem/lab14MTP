# Prompt Log — Лабораторная работа №14

**Студент:** Сторожетдинов Максим  
**Вариант:** 7 — Анализ вакансий hh.ru  
**Инструмент:** Claude Code (claude-sonnet-4-6)

---

## Задание 1: Распределённый сборщик на Go (etcd)

### Промпт 1
**Инструмент:** Claude Code  
**Промпт:** Изучи все файлы в папке лабораторной №14 и дай знать когда будешь готов.

**Результат:** Claude изучил PDF с требованиями и файл с информацией. Уточнил выбор блока заданий (повышенная сложность) и включение Rust.

### Промпт 2
**Промпт:** Создай структуру Go-модуля для сборщика, hh.ru API клиент, etcd-координатор с distributed lock для шардирования.

**Результат:** Получил `hh/client.go` с полным клиентом hh.ru API, `etcd/coordinator.go` с TryLock по шардам, `main.go` с логикой распределённого сбора и graceful shutdown.

**Что пришлось исправить:** 
- Ошибка типов в `window.go` — `byArea` использовал смешанные указатели, исправил инициализацию `areaAccum`.

### Итого
- Количество промптов: 3
- Что пришлось исправлять вручную: ничего
- Время: ~25 мин

---

## Задание 2: Оконная агрегация (tumbling window)

### Промпт 1
**Промпт:** Создай `window/window.go` — TumblingWindow с агрегацией по регионам, зарплатам и навыкам.

**Результат:** Реализован `TumblingWindow` с goroutine, ticker, буфером и flush по таймеру или Stop.

**Результат тестов:** 3 теста Go прошли сразу.

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 3: Apache Arrow Flight RPC

### Промпт 1
**Промпт:** Создай `arrow/server.go` — Arrow Flight RPC сервер, отдающий агрегированные окна вакансий. Python-клиент `arrow_client.py`.

**Результат:** Go-сервер использует `flight.NewRecordWriter`, Python-клиент — `pyarrow.flight.connect`.

**Что пришлось исправить:**
- Первый вариант arrow/server.go использовал несуществующие `flight.DataBody` и `flight.NewDataWriter` — заменил на правильный API с `flight.NewRecordWriter`.
- Добавил импорт `ipc` для `ipc.WithSchema` и `ipc.WithAllocator`.

### Итого
- Количество промптов: 2
- Время: ~15 мин

---

## Задание 4: Rust-библиотека для валидации

### Промпт 1
**Промпт:** Создай Rust-библиотеку `vacancy_validator` с C ABI (для cgo) и PyO3 (для Python). Правила валидации: длина имени, диапазон зарплат, наличие area_id.

**Результат:** `src/lib.rs` с `validate_vacancy()`, `free_validation_result()`, PyO3-модулем и Rust-тестами. C-заголовок `vacancy_validator.h` для cgo.

**Go-интеграция:** build tag `rust` для cgo-версии, `!rust` — для Go-fallback.

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 5: Kubernetes + Docker

### Промпт 1
**Промпт:** Создай Docker Compose с etcd + 2 коллектора + анализатор + дашборд. Kubernetes манифесты с HPA.

**Результат:** `docker-compose.yml`, Dockerfile для каждого компонента, `k8s/collector.yaml` с HPA (min 2, max 6 pods, CPU > 60%).

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Задание 6: Streamlit-дашборд

### Промпт 1
**Промпт:** Создай Streamlit-дашборд с авто-обновлением: вакансии по регионам (bar), зарплаты (histogram + box), топ навыков (bar), топ работодателей (table).

**Результат:** `src/dashboard/app.py` с `st.cache_data(ttl=30)`, автообновлением через `st.rerun()`, sidebar с настройками.

### Итого
- Количество промптов: 1
- Время: ~10 мин

---

## Python-анализатор (задания 4-9 базового блока)

### Промпт 1
**Промпт:** Создай `analysis.py` с загрузкой JSONL, очисткой (дубликаты, структурные поля), агрегацией Polars, Parquet, DuckDB с замером производительности.

**Результат:** Базовая структура работала, но DuckDB-запросы ссылались на `area_name`, которой не было в Parquet (данные хранились как struct).

**Исправление:** Нормализация структурных полей в `clean_data` — unnest `area`, `employer`, `snippet`, `salary` через `struct.field()`.

### Промпт 2 (тесты)
**Промпт:** Создай pytest-тесты для всех функций анализа.

**Результат:** 8 тестов, все зелёные. Ошибка: использовал `.empty` (Pandas) вместо `.is_empty()` (Polars) — исправил.

### Итого
- Количество промптов: 3
- Что пришлось исправлять: нормализацию struct-полей, `.empty` → `.is_empty()`
- Время: ~20 мин

---

## Итоговая статистика

| Метрика | Значение |
|---------|---------|
| Общее количество промптов | ~15 |
| Время выполнения | ~100 мин |
| Тестов Go | 9 (все зелёные) |
| Тестов Python | 8 (все зелёные) |
| Тестов Rust | 6 (в коде, cargo test) |
| Исправлений вручную | 3 |

## Инструменты

- **Claude Code** (claude-sonnet-4-6) — основной инструмент разработки
- **Go 1.21** — сборщик
- **Python 3.13** — анализатор и дашборд
- **Rust** — библиотека валидации
- **Docker / Kubernetes** — инфраструктура
